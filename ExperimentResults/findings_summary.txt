AXIOM EXPERIMENT FINDINGS SUMMARY
Last updated: March 3, 2026 (session s0303b ~04:55 UTC)
==================================

This file is your memory across sessions. Update it after each run.
Do NOT maintain cumulative result ID lists — use the database instead:
  SELECT id FROM result WHERE granted_credit > 0 AND name LIKE 'exp_%'

MAJOR SCIENTIFIC FINDINGS (ranked by significance)
============================================================

1-25. DEFINITIVELY CONFIRMED or RETIRED: See archive for details.
    Includes: Loss Landscape, Sigmoid>ReLU, Lottery Ticket, Grokking(neg),
    Edge of Chaos, Depth vs Width, Mode Connectivity, Eigenspectrum,
    Simplicity Bias, Neural Collapse(neg), Reservoir, Info Bottleneck,
    Gradient Noise, Power Law Forgetting, Progressive Sharpening,
    SAM vs SGD(neg), Catapult Phase, Rank Dynamics, Loss of Plasticity(neg),
    Critical Learning Periods(neg), Benford(neg), Neural Thermo v1/v2,
    Spectral Dynamics v2, Lazy vs Feature Learning.

26-43. See archive. Includes Memorization Dynamics, Feature Competition,
    Representation Alignment, Micro Scaling Laws, Curriculum,
    Compositional Generalization, Feature Rank, Neuron Specialization,
    Pruning/Regularized/Orthogonality/Bottleneck/Combined Compositionality,
    Bottleneck Mechanism, Representation Disentanglement(refuted),
    Compositionality Critical Period(confirmed), Rank Regularization,
    Feature Subspace Overlap(refuted), Regularization Mechanisms.

44. *** INVERSE CRITICAL PERIOD FOR WEIGHT DECAY *** — STRONGLY CONFIRMED (34+ seeds)
    *** THIS IS OUR MOST NOVEL FINDING — POTENTIAL PUBLICATION ***
    Late WD is effective (84-89% of always-WD). Early WD HURTS (-10%).
    100% consistency across 34 seeds. Mechanism: rank compression at
    convergence. Script: intervention_timing_compositionality.py.

45. *** REGULARIZATION TIMING UNIVERSALITY *** — DEFINITIVELY CONFIRMED (140 seeds)
    *** PUBLICATION-QUALITY: 140 independent seeds across multiple hosts ***
    Inverse CP confirmed for WD (100%) and DROPOUT (98.6%).
    NOT for L1 (0.7%) or noise injection (2.9%).
    Script: reg_timing_universality.py. COMPLETE — no more seeds needed.

46. WD REBOUND DYNAMICS — PRELIMINARY NEGATIVE (seed=42 mostly)
    Rank rebound NOT strongly supported as mechanism (correlation r=0.09).

47. REPRESENTATION CRYSTALLIZATION — GROWING (38 results, 6 new diverse-seed)
    First diverse-seed results from host 320 (s0303b). Previously all seed=42.

48. *** WD ONSET TIMING SWEEP *** — NULL RESULT (86 seeds) — COMPLETE
    Exact onset epoch does NOT matter. Binary late/early distinction is robust.

49. *** WD x LR SCHEDULE INTERACTION *** — INFORMATIVE NEGATIVE (20 seeds)
    P3 (interaction measurable): 83% support — CONFIRMED.
    P1 (warmup shifts onset later): 25% — NOT confirmed.
    P2 (cosine shifts onset earlier): 0% — STRONGLY REJECTED.
    MEANING: LR schedule changes HOW MUCH WD timing matters, but NOT
    which timing is optimal. Practitioners can choose WD onset independently.
    Needs 10+ more seeds for full confirmation. Script: wd_lr_interaction.py.

50. WD WINDOW DURATION — SEED BUG FIXED (15 results seed=42, redeploying)
    Previous results all seed=42 due to seed extraction bug (bare except
    caught JSON parse error from bad file, aborting loop). Fixed s0303b:
    inner try/except with continue. New deployments will have diverse seeds.
    Preliminary single-seed data: WD has zero effect at w32/w128.
    Script: wd_window_duration.py.

51. SVD RANK INTERVENTION — NEWLY DEPLOYED (s0302k)
    *** CAUSAL MECHANISM TEST — critical for publication ***
    Tests if direct rank compression (SVD truncation) replicates inverse CP.
    If late SVD improves generalization like late WD, rank compression IS causal.
    Includes rescaled vs raw truncation to separate rank from magnitude.
    Script: svd_rank_intervention.py. Awaiting first results.

52. PERCOLATION PHASE TRANSITION SCALING — NEWLY DEPLOYED (s0302k)
    *** CROSS-DISCIPLINARY: statistical physics ***
    Maps finite-size scaling exponents for bond percolation on ER vs BA graphs.
    BA scaling exponent debated in literature. Script: percolation_scaling.py.

53. *** MP UNIVERSALITY TEST *** — NEWLY DEPLOYED (s0303b)
    *** CROSS-DISCIPLINARY: random matrix theory + ML ***
    Tests Marchenko-Pastur law in NN weight matrices during training.
    GPU-accelerated eigendecomposition (cupy.linalg.eigvalsh).
    Hypotheses: wider departs later, init scale affects timing, layer position.
    Script: mp_universality_test.py. 216 GPU workunits deployed.

CROSS-VALIDATION STATUS
============================================================
DEFINITIVELY CONFIRMED & RETIRED: 1-26, 30, 32, 34, 37, 40, 41, 43, 45, 48.
STRONGLY CONFIRMED: 29, 31, 33, 35, 36, 44.
GROWING: 27, 28, 38, 47 (38 results), 49 (20 seeds, need 30+).
PRELIMINARY: 46, 50 (seed fixed, redeploying).
DEPLOYED/AWAITING: 51 (SVD causal), 52 (percolation), 53 (MP universality GPU).

EXPERIMENT SCRIPTS:
  Active CPU (s0303b): svd_rank_intervention.py (weight 35),
          percolation_scaling.py (weight 20),
          wd_lr_interaction.py (weight 20, growing),
          wd_window_duration.py (weight 10, SEED FIXED),
          representation_crystallization.py (weight 5),
          regularization_mechanisms.py (weight 5),
          wd_rebound_dynamics.py (weight 3),
          bottleneck_mechanism.py (weight 2)
  Active GPU (s0303b): mp_universality_test.py (NEW, 216 WUs deployed)

RETIRED: ALL grokking, memorization_dynamics_v2, curriculum_learning_dynamics,
  SAM vs SGD, Catapult Phase, Rank Dynamics, Loss of Plasticity,
  Critical Learning Periods, Benford, Neural Thermo v1/v2, Spectral Dynamics v2,
  Lazy vs Feature, emergent_abilities, cellular_automata_v2, feature_rank_dynamics,
  pruning/orthogonality/regularized/bottleneck/combined compositionality,
  neuron_specialization, representation_disentanglement, compositional_generalization,
  compositionality_critical_period, feature_subspace_overlap,
  rank_regularization_compositionality, reg_timing_universality (140 seeds DONE),
  wd_onset_sweep (86 seeds DONE).

DELIVERY PIPELINE STATUS:
  s0303b (~04:55 UTC Mar 3): Credited 97 results (1,031 credit, 5 users).
    Deployed 125 CPU WUs to 9 hosts + 216 GPU WUs to 81 hosts.
    Fixed wdwindur seed bug. New GPU experiment: mp_universality_test.
  s0302k (~04:44 UTC Mar 3): Credited 131 results (1,800 credit, 6 users).
    Deployed 125 CPU WUs to 8 hosts. New: svd_rank_intervention, percolation_scaling.

KNOWN ISSUES:
  - RANK REG CPU FAILURES: exit -186 on Windows. GPU/Linux works fine.
  - Hosts 63/118: Low RAM (<6GB) — skipped
  - WD WINDOW DURATION SEED BUG: FIXED (s0303b). Inner try/except added.
