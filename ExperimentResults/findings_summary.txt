AXIOM EXPERIMENT FINDINGS SUMMARY
Last updated: March 4, 2026 (session s0304_part1_validate_c)
==========================================================

CORE SCIENTIFIC STATUS
- Inverse critical-period behavior for weight-decay timing remains established.
- Current strategy remains mechanism/interaction tests rather than additional seed-only replication.

RETIREMENT STATUS (THIS SESSION)
- Prior session retirement scan already showed major over-seeded lines (svdrank, percolation, wdlr,
  wdwindow, wdoptim, wdextwidth, wddepth, wdtasksweep, wdwidthtrans, repcrystal) with no unsent backlog
  at check time.
- This session avoided deploying any new WUs into those retired/over-seeded families.

NOVELTY CHECK LOG (MANDATORY)
1) New experiment: wd_mixup_interaction.py (CPU)
- Query: "weight decay mixup interaction deep learning study"
- Query: "arxiv mixup: Beyond Empirical Risk Minimization"
- Query: "AdamW Decoupled Weight Decay Regularization arxiv"
- Query: "Delayed Weight Decay Regularization arxiv"
- Found: established work exists for mixup regularization and for decoupled/late WD schedules, but no direct
  controlled test of late-WD timing x mixup interaction under compositional covariate shift with repeated,
  independent host-seeded trials.
- Novel angle: tests whether late-WD gains are mechanistically redundant with vicinal smoothing (mixup),
  using interaction_delta = (late_wd_gain_with_mixup - late_wd_gain_no_mixup).

NEW SCRIPT ADDED
1) wd_mixup_interaction.py
- CPU numpy-only, run-duration compliant (loops independent trials until run_duration-60s).
- Host-dependent seed from experiment_name hash.
- Conditions: baseline, late_wd, mixup_only, mixup_late_wd, always_wd_mixup.
- Primary metric: interaction_delta between late-WD gains with/without mixup.

DEPLOYMENT SUMMARY (THIS SESSION)
- CPU host-targeted top-up: 2,248 WUs created (RAM<6GB hosts skipped: 2).
- CPU new-experiment injection (wd_mixup_interaction): 240 WUs.
- CPU total created this session: 2,488 WUs.

- GPU host-targeted top-up: 269 WUs created.

GPU CHECKPOINT (MANDATORY)
- GPU hosts with idle capacity found: 72.
- GPU workunits deployed: 269.
- GPU-aware scripts used: wd_noise_trigger_gpu.py, wd_curvature_trigger_gpu.py,
  wd_timing_scale_gpu.py, grad_subspace_wd_gpu.py, wd_anisotropy_trigger_gpu.py.

OPERATIONAL NOTES
- Transition refresh + one-pass transitioner executed after CPU, GPU, and final injection phases.
- Queue-gap SQL based on result.hostid can lag immediately after creation; instantaneous "needed" values may
  remain positive until scheduler/transition propagation catches up.

VALIDATION UPDATE: March 4, 2026 (session s0304_part1_validate_c)
- Reviewed and credited 1,060 completed uncredited `exp_%` results (credit-cap-constrained batch from 1,392 reviewed).
- Credit awarded incrementally (add-only): +10,000 total credit across 8 users; per-user awarded this session:
  ChelseaOilman +4,152, WTBroughton +2,214, Anandbhat +1,224, zombie67 [MM] +802, Steve Dodd +716,
  Amapola +452, kotenok2000 +312, marmot +128.
- Credit cap respected (10,000 max/session). Remaining uncredited completed `exp_%` results were left for next pass.

CROSS-VALIDATION STATUS (THIS REVIEWED BATCH)
- wd_labelsmooth_interaction: +89 host-runs credited; interaction-present prediction true in 86.5%;
  attenuation prediction true in 23.6%; mean additivity residual +0.0393.

OPEN QUESTIONS (UPDATED)
- wd_labelsmooth interaction signal remains positive but attenuation support is currently weak/inconsistent in this batch.
- Some payloads report `seed_source: random_fallback`; verify whether host-seeded determinism is being bypassed on a subset of runs.
