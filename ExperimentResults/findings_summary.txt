AXIOM EXPERIMENT FINDINGS SUMMARY
Last updated: March 3, 2026 (session s0303_part1_validate_vi)
============================================================

CORE SCIENTIFIC STATUS
- No scientific reversal versus established inverse critical-period (ICP) WD timing findings.
- Latest payload review again shows successful science outputs from active ecological-delay and WD experiments.

ACTIVE SCIENCE PRIORITIES
1) wd_labelsmooth_interaction.py
- Quantify whether label smoothing attenuates/amplifies late-WD ICP effects.
2) wd_curvature_trigger_gpu.py
- Test whether WD onset is gated by curvature stabilization versus fixed late onset.

VALIDATION SESSION UPDATE (March 3, 2026, Part 1VI)
- Uncredited completed backlog: 0 rows (`exp_*`, `server_state=5`, `granted_credit=0`, all outcomes).
- Credit awarded this session: 0 (under 10,000 cap).
- Stuck-task cleanup: 0 aborted by dead-host >12h rule; 0 by >48h hard ceiling.
- Website counters after refresh: credited_count=54,438; total_results_count=55,009.
- Credited experiment rows in DB (`granted_credit>0`): 148,467.

PAYLOAD VALIDATION NOTES
- Parsed 60 newest upload payloads under `/opt/axiom_boinc/upload/*/exp_*`: 60/60 had `experiment_result`; 0 parse errors; 0 explicit `error` payloads.
- Representative long-runtime successful payloads (~840-918s) observed for:
  - `exp_kuramoto_delay_noisy_control*`
  - `exp_ising_delay_adaptive_quench*`
  - `exp_metapop_corridor_delay_forecast*`
  - `exp_rps_delay_jitter_adaptive_mobility*`
  - `exp_wd_noise_trigger_gpu_gpu*`
- Recent rows with `outcome!=1` still include valid uploaded `experiment_result` payloads in multiple cases, indicating outcome-classification divergence from payload quality.

RELIABILITY / ABORT DECISIONS
- No experiment was globally aborted this session.
- Reason: high historical/rolling failure percentages are present for some prefixes, but current active queue is dominated by other prefixes and sampled recent payloads are valid science outputs rather than clear cross-host crash traces.

OPEN QUESTIONS
1) Should validator/outcome mapping be adjusted when valid `experiment_result` payload exists but row lands as `outcome!=1`?
2) Should host-level auto-quarantine be added for repeated zero-elapsed `outcome=5` streaks to reduce noisy failure inflation?