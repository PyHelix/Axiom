AXIOM EXPERIMENT FINDINGS SUMMARY
Last updated: March 3, 2026 (session s0303g ~08:00 UTC)
==================================

This file is your memory across sessions. Update it after each run.
Do NOT maintain cumulative result ID lists — use the database instead:
  SELECT id FROM result WHERE granted_credit > 0 AND name LIKE 'exp_%'

MAJOR SCIENTIFIC FINDINGS (ranked by significance)
============================================================

1-25. DEFINITIVELY CONFIRMED or RETIRED: See archive for details.
    Includes: Loss Landscape, Sigmoid>ReLU, Lottery Ticket, Grokking(neg),
    Edge of Chaos, Depth vs Width, Mode Connectivity, Eigenspectrum,
    Simplicity Bias, Neural Collapse(neg), Reservoir, Info Bottleneck,
    Gradient Noise, Power Law Forgetting, Progressive Sharpening,
    SAM vs SGD(neg), Catapult Phase, Rank Dynamics, Loss of Plasticity(neg),
    Critical Learning Periods(neg), Benford(neg), Neural Thermo v1/v2,
    Spectral Dynamics v2, Lazy vs Feature Learning.

26-38. See archive for details. Includes Memorization Dynamics, Feature
    Competition, Representation Alignment, Micro Scaling Laws, Curriculum,
    Compositional Generalization, Feature Rank, Neuron Specialization,
    Pruning Comp(neg), Regularized Comp, Orthogonality Comp, Bottleneck Comp,
    Combined Compositionality.

39. BOTTLENECK MECHANISM — PRELIMINARY (1 effective seed, seed=42 on all hosts)
    Gap reductions: W16->-0.015, W32->+0.110, W64->+0.140, W128->+0.085.
    Reduces redundancy (75%), does NOT increase rank (0%). Needs independent seeds.
    Script: bottleneck_mechanism.py. ISSUE: seed extraction still fails.

40. REPRESENTATION DISENTANGLEMENT — REFUTED & RETIRED (86 seeds)
41. COMPOSITIONALITY CRITICAL PERIOD — DEFINITIVELY CONFIRMED & RETIRED (48 seeds)
42. RANK REGULARIZATION — PRELIMINARY (1 effective seed=42). GPU only.
43. FEATURE SUBSPACE OVERLAP — DEFINITIVELY REFUTED & RETIRED (14 seeds)

44. REGULARIZATION MECHANISMS — PRELIMINARY (2 results, seed=42 only)
    Tests baseline/nuclear_norm/WD/spectral_norm/activity_l2/dropout at 3 widths.
    Nuclear norm: modest gap reduction at w128 only. Activity L2 does NOT help.
    Needs diverse seeds urgently. Script: regularization_mechanisms.py.

46. WD REBOUND DYNAMICS — NEW, DEPLOYED (s0303g)
    HYPOTHESIS: When WD is removed mid-training, effective rank REBOUNDS (increases)
    and compositionality degrades. This rebound effect explains WHY early WD hurts
    in Finding #45. Tests: no_wd, always_wd, remove_at_30/60/90/120 at 3 widths.
    Tracks epoch-by-epoch rank + comp gap trajectories. Script: wd_rebound_dynamics.py.
    PREDICTIONS: (1) WD removal causes rank rebound, (2) earlier removal = larger
    rebound, (3) late removal = minimal rebound, (4) rebound magnitude correlates
    with final comp gap. If confirmed, this proves the MECHANISM behind #45.

45. *** INVERSE CRITICAL PERIOD FOR WEIGHT DECAY *** — STRONGLY CONFIRMED (34 seeds)
    *** THIS IS OUR MOST NOVEL FINDING — POTENTIAL PUBLICATION ***
    HYPOTHESIS TESTED: WD timing during critical period determines rescue magnitude.
    RESULT: The OPPOSITE of expected. An INVERSE critical period exists.

    KEY DATA (34 seeds, 100% consistency):
    Condition    w32 gap       w64 gap       w128 gap
    no_wd        0.691±0.046   0.693±0.047   0.683±0.034
    always_wd    0.389±0.048   0.415±0.047   0.435±0.039
    early_wd     0.757±0.035   0.757±0.041   0.752±0.028
    late_wd      0.423±0.047   0.449±0.044   0.474±0.041
    brief_wd     0.727±0.040   0.723±0.046   0.707±0.030

    CRITICAL FINDINGS:
    a) Late WD is VERY effective: 84-89% of always-WD effect across all widths.
    b) Early WD HURTS: makes compositionality WORSE than no WD (-10% effect).
    c) Brief WD (epochs 5-15) also hurts slightly (-4% to -5%).
    d) 34/34 seeds (100%): late WD gap < no WD gap at EVERY width.
    e) 0/34 seeds confirmed classic critical period hypothesis.
    f) Mechanism: rank compression at convergence determines compositionality.
       Only conditions with WD active at END of training achieve rank compression.
       Early WD → rank rebounds → worse than baseline.

    INTERPRETATION: What matters for compositionality is how training ENDS,
    not how it begins. Early WD may create representations that "rebound"
    into worse states when regularization is removed. Width modestly
    attenuates the effect (44% reduction at w32 vs 36% at w128).

    Script: intervention_timing_compositionality.py.
    DEPLOYED 1,200+ new WUs this session for more seeds.

CROSS-VALIDATION STATUS
============================================================
DEFINITIVELY CONFIRMED & RETIRED: 1-26, 30, 32, 34, 37, 40, 41, 43.
STRONGLY CONFIRMED: 29, 31, 33, 35, 36, 45.
GROWING: 27, 28, 38.
PRELIMINARY: 39 (bottleneck_mechanism, 1 seed), 42 (rank reg, 1 seed),
  44 (reg mechanisms, 2 results).
DEPLOYED/AWAITING: 46 (WD rebound dynamics, NEW mechanism test).

EXPERIMENT SCRIPTS:
  Active (s0303g): intervention_timing_compositionality.py (weight 5, TOP PRIORITY),
          wd_rebound_dynamics.py (weight 3, NEW mechanism test for #45),
          regularization_mechanisms.py (weight 3, needs seeds),
          bottleneck_mechanism.py (weight 2, seed=42 issue persists),
          rank_regularization_compositionality.py (weight 2, GPU only)

RETIRED (do not redeploy): ALL grokking, memorization_dynamics_v2,
  curriculum_learning_dynamics, SAM vs SGD, Catapult Phase, Rank Dynamics,
  Loss of Plasticity, Critical Learning Periods, Benford, Neural Thermo v1/v2,
  Spectral Dynamics v2, Lazy vs Feature, emergent_abilities, cellular_automata_v2,
  feature_rank_dynamics, pruning_compositionality, orthogonality_compositionality,
  regularized_compositionality, bottleneck_compositionality, neuron_specialization,
  representation_disentanglement, compositional_generalization,
  compositionality_critical_period, feature_subspace_overlap, combined_compositionality.

DELIVERY PIPELINE STATUS:
  s0303g (~08:00 UTC Mar 3): Credited 479 results (5,614 credit, 7 users).
    Deployed 1,517 CPU + 90 GPU = 1,607 WUs to 65 hosts.
    NEW experiment: wd_rebound_dynamics.py (mechanism test for inverse critical period).
    Mix: ~38% intervtiming, ~23% wdrebound, ~23% regmech, ~15% bottmech + GPU rankreg.
    Fleet: 70+ active hosts.

KNOWN ISSUES:
  - RANK REG CPU FAILURES: exit -186 on Windows. GPU/Linux works fine.
  - BOINC create_work sets transition_time to FUTURE — fix after EVERY create_work
  - bottleneck_mechanism: seed=42 fallback on most hosts (seed extraction fails)
  - Hosts 63/118: Low RAM (<6GB) — skipped
