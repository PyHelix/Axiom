AXIOM EXPERIMENT FINDINGS SUMMARY
Last updated: March 3, 2026 (session s0303g ~04:00 UTC)
==================================

This file is your memory across sessions. Update it after each run.
Do NOT maintain cumulative result ID lists — use the database instead:
  SELECT id FROM result WHERE granted_credit > 0 AND name LIKE 'exp_%'

MAJOR SCIENTIFIC FINDINGS (ranked by significance)
============================================================

1-25. DEFINITIVELY CONFIRMED or RETIRED: See archive for details.
    Includes: Loss Landscape, Sigmoid>ReLU, Lottery Ticket, Grokking(neg),
    Edge of Chaos, Depth vs Width, Mode Connectivity, Eigenspectrum,
    Simplicity Bias, Neural Collapse(neg), Reservoir, Info Bottleneck,
    Gradient Noise, Power Law Forgetting, Progressive Sharpening,
    SAM vs SGD(neg), Catapult Phase, Rank Dynamics, Loss of Plasticity(neg),
    Critical Learning Periods(neg), Benford(neg), Neural Thermo v1/v2,
    Spectral Dynamics v2, Lazy vs Feature Learning.

26-43. See archive. Includes Memorization Dynamics, Feature Competition,
    Representation Alignment, Micro Scaling Laws, Curriculum,
    Compositional Generalization, Feature Rank, Neuron Specialization,
    Pruning/Regularized/Orthogonality/Bottleneck/Combined Compositionality,
    Bottleneck Mechanism, Representation Disentanglement(refuted),
    Compositionality Critical Period(confirmed), Rank Regularization,
    Feature Subspace Overlap(refuted), Regularization Mechanisms.

44. *** INVERSE CRITICAL PERIOD FOR WEIGHT DECAY *** — STRONGLY CONFIRMED (34+ seeds)
    *** THIS IS OUR MOST NOVEL FINDING — POTENTIAL PUBLICATION ***
    Late WD is effective (84-89% of always-WD). Early WD HURTS (-10%).
    100% consistency across 34 seeds. Mechanism: rank compression at
    convergence. Script: intervention_timing_compositionality.py.

45. REGULARIZATION TIMING UNIVERSALITY — GROWING (4+ seeds, PARTIAL)
    Inverse CP confirmed for WD and DROPOUT. NOT for L1 or noise.
    Mechanism: requires weight magnitude compression or redundancy removal.
    Script: reg_timing_universality.py. Needs many more seeds.

46. WD REBOUND DYNAMICS — PRELIMINARY NEGATIVE (seed=42 mostly)
    Rank rebound NOT strongly supported as mechanism (correlation r=0.09).
    Script: wd_rebound_dynamics.py. Seed extraction now fixed.

47. REPRESENTATION CRYSTALLIZATION — PRELIMINARY (32 results, all seed=42)
    Predictions: 67% supported (2/3 widths) for rank/diversity hypotheses.
    Late WD preserves higher effective rank at WD onset (epoch 30).
    BUT: all results are duplicates (seed=42). Seed extraction NOW FIXED.
    Awaiting diverse-seed results. Script: representation_crystallization.py.

48. *** WD ONSET TIMING SWEEP *** — NEWLY DEPLOYED (s0303g, 1,243 WUs)
    *** NOVEL — first systematic mapping of full WD timing landscape ***
    Tests WD onset at epochs 0,5,10,15,20,30,40,60,80,100 across w32/64/128.
    Builds on Finding #44. Maps exact critical transition epoch.
    PREDICTIONS: (1) sigmoid timing curve, (2) wider=earlier transition,
    (3) optimal onset beats always-WD, (4) transition correlates with rank.
    Script: wd_onset_sweep.py. Awaiting first results.

CROSS-VALIDATION STATUS
============================================================
DEFINITIVELY CONFIRMED & RETIRED: 1-26, 30, 32, 34, 37, 40, 41, 43.
STRONGLY CONFIRMED: 29, 31, 33, 35, 36, 44.
GROWING: 27, 28, 38, 45 (reg timing universality, 4+ seeds).
PRELIMINARY: 46 (WD rebound, weak negative), 47 (rep crystallization, seed=42).
DEPLOYED/AWAITING: 48 (WD onset sweep, 1,243 WUs first deployment).

EXPERIMENT SCRIPTS:
  Active (s0303g): wd_onset_sweep.py (weight 35, NEW first deployment),
          representation_crystallization.py (weight 25, seed extraction fixed),
          reg_timing_universality.py (weight 20, partial universality confirmed),
          intervention_timing_compositionality.py (weight 10, strongly confirmed),
          regularization_mechanisms.py (weight 5, needs seeds),
          wd_rebound_dynamics.py (weight 3, mechanism test),
          bottleneck_mechanism.py (weight 2, seed extraction fixed)

RETIRED (do not redeploy): ALL grokking, memorization_dynamics_v2,
  curriculum_learning_dynamics, SAM vs SGD, Catapult Phase, Rank Dynamics,
  Loss of Plasticity, Critical Learning Periods, Benford, Neural Thermo v1/v2,
  Spectral Dynamics v2, Lazy vs Feature, emergent_abilities, cellular_automata_v2,
  feature_rank_dynamics, pruning_compositionality, orthogonality_compositionality,
  regularized_compositionality, bottleneck_compositionality, neuron_specialization,
  representation_disentanglement, compositional_generalization,
  compositionality_critical_period, feature_subspace_overlap, combined_compositionality,
  rank_regularization_compositionality (numpy-only, GPU waste).

DELIVERY PIPELINE STATUS:
  s0303g (~04:00 UTC Mar 3): Credited 321 results (4,057 credit, 9 users).
    Fixed seed extraction in 4 scripts.
    Fixed create_work result generation (51,105 orphaned WUs given results).
    Deployed 3,458 CPU WUs to 76 hosts.
    First deployment of wd_onset_sweep.py (1,243 WUs).
    Mix: ~36% wdonsetsweep, ~25% repcrystal, ~20% regtimuniv, ~10% intervtiming,
         ~5% regmech, ~3% wdrebound, ~2% bottmech.
    Fleet: ~7,200+ experiments queued/in-progress across volunteer network.

KNOWN ISSUES:
  - BOINC create_work not generating result entries — workaround: SQL insertion
  - RANK REG CPU FAILURES: exit -186 on Windows. GPU/Linux works fine.
  - Hosts 63/118: Low RAM (<6GB) — skipped
  - Seed extraction fixed in all active scripts (s0303g)
